# 위키드스톰 홈페이지 재구축

위키드스톰(Wicked Storm Inc.)은 교육 분야 학습데이터·AI 회사입니다. 이 저장소에서 공식 홈페이지를 새로 만듭니다. 비공개 저장소입니다.

## 먼저 읽을 것
- **재구축안(설계 문서)**: `docs/redesign/analysis/index.html`. 브라우저로 열어 봅니다. 이 문서가 방향의 기준입니다.
- **레퍼런스 조사**: `docs/redesign/research.md`. 캡처는 `docs/redesign/research-shots/`.
- **제품 화면 원본**: `docs/source/`. 화면 속 인물·교과·수치는 모두 시연용입니다.
- **지금 운영 중인 사이트**: `wickedstormkr/homepage_renewal`. 공개 저장소이고 https://wickedstormkr.github.io/homepage_renewal/ 에 올라가 있습니다.
  - 문구, 네 언어 번역, 이미지, 소식, 카탈로그 PDF, 기업영상의 원본이 거기 있습니다.
  - 그 저장소는 읽기만 하고 고치지 않습니다.

## 목표
- 네 가지 목적을 한 사이트에서 이룹니다: 공공·교육청 신뢰, 도입·시연 문의, 해외 파트너·박람회(하노이 VIETEDU 등), 채용·기술 브랜드.
- 톤은 정제된 제품 이야기와 데이터 아트입니다.

## 오프닝: 여섯 장면 스크롤 이야기
1. **실시간 수집**: "학습의 모든 순간을 성장의 데이터로 설계합니다"와 Lecognizer 실시간 수집 패널(xAPI 문장이 한 줄씩 들어옴). 목록을 지나간 한 건은 그 활동 종류 색의 별이 되어 배경 성운에 자리 잡음
2. **국제 표준**: 흩어진 점이 기록 행에 착지. 위 세 줄 xAPI(actor · verb · object), 아래 세 줄 1EdTech Caliper(actor · action · object), 줄마다 부가 정보(context · extensions: 시각, 기기·환경, 세션, 재생 위치, 시도 횟수, 교육과정 맥락). 부가 정보를 함께 수집한다는 것이 핵심
3. **프로파일링**: 점이 1EdTech CASE 성취 항목(지표) 체계로 모임. 지표를 설정하고 학습자를 프로파일링해야 학습데이터가 의미 있는 값이 됨(우리의 강점, 특허 제10-2767110호)
4. **이상 신호**: Lecognizer AI가 한 항목을 밝힘. 입자가 주차별 막대로 모이고, 실제 이상 탐지 화면으로 착지(화면이 막대를 가리지 않게)
5. **사람의 판단**: 이상 신호가 근거와 함께 LearnHubble AI 교수자 화면에 도착하고, 교수자가 보충 구간을 설계
6. **다음 학기**: "이번 학기의 학습데이터가 다음 학기 교육을 설계합니다", 점으로 그린 학습의 선순환 고리, 도입 문의·시연 요청

데이터 아트는 진짜 데이터 구조로만 그립니다. 입자 하나가 xAPI 문장 하나, 입자가 모이는 자리가 CASE 성취 항목, 색이 활동 종류(시청·응답·제출·질문)입니다.

## 원칙
1. 이야기는 한 번, 끝은 제품 화면.
2. 글자는 모두 HTML. 그림에 글자를 굽지 않습니다.
3. 움직임은 설명할 때만. '움직임 멈춤' 버튼으로 언제든 멈추고, prefers-reduced-motion을 따릅니다.
4. 폰은 같은 이야기를 장면으로. 데스크톱 연출을 줄여 넣지 않고 폰에 맞게 다시 구성합니다.
5. 주장은 확인할 수 있게. 인증·특허·실적마다 원본으로 가는 링크를 둡니다.

## 기술
- **빌드**: Astro(정적 출력) + TypeScript. 경로는 `/`(한국어), `/en/`, `/ja/`, `/vi/`.
- **연출**:
  - GSAP 3.13(ScrollTrigger·SplitText). Lenis는 데스크톱만.
  - 데이터 아트는 3층: OGL(WebGL2) → 캔버스 → 미리 그린 그림. 맨 아래 층만으로도 이야기가 완결되어야 합니다.
  - 히어로에 영상 자동 재생을 쓰지 않습니다. iOS 저전력 모드가 막습니다.
- **성능 예산**:
  - 폰 LCP 2.5초 이하, CLS 0.1 이하, INP 200ms 이하.
  - 첫 로드 JS 90KB(gzip) 이하.
  - WebGL은 첫 화면이 뜬 뒤, 성능이 되는 기기에서만 불러옵니다.
- **접근성**:
  - KWCAG 2.2(WCAG 2.2 AA 수준)를 지킵니다.
  - 자동으로 움직이거나 바뀌는 것에는 정지 수단을 둡니다(KWCAG 6.2.2).
  - 명도 대비 4.5:1, 키보드로 장면 이동, 장면마다 글 설명.
- **반응형**:
  - 폰 699px 이하, 태블릿 700~1023px, 데스크톱 1024px 이상.
  - 터치에서 누르는 곳은 44px 이상, 본문은 15px 이상.
  - 그림 속 글자는 데스크톱에서만 쓰고 11px 이상으로 합니다.
- **자동 점검(CI)**:
  - 폭 360·390·430·768·820·1024·1280·1440·1920 × 네 언어에서 확인합니다: 가로 넘침, 칸 밖 글자, 11px 미만 글자, 누르는 곳 크기, 마지막 줄 한 단어.
  - 그 밖에 axe, Lighthouse 예산, 깨진 링크를 검사합니다.
- **문의 폼**: 지금 운영 중인 서버를 그대로 씁니다. 엔드포인트는 homepage_renewal의 `js/site-config.js`에 있습니다.

## 사실 (지금 사이트 기준, 바꾸지 말 것)
- **Lecognizer**: AI 기반 이상 탐지를 갖춘 국제 표준 학습데이터 저장소(LRS). GS 인증 1등급.
- **Lecognizer AI**: 학습데이터의 이상 신호 탐지.
  - 특허 제10-2883249호: xAPI 학습 행동 데이터 기반 이상 학습 탐지 시스템.
  - 특허 제10-2767110호: 학습자 성향 분석을 위한 LLM 기반 학습자 프로파일링 방법.
- **LearnHubble AI**: AI와 상호작용하는 맞춤형 교수·학습 플랫폼. 포트폴리오, 과제 힌트, 역량맵, 강의실 AI 패널.
- **표준**:
  - 적용: xAPI, Caliper, 1EdTech CASE
  - 적용 예정: Open Badges 3.0, CLR 2.0, EduAPI, Digital Credentials
  - 설계 참조: OneRoster, LTI, QTI
- **1EdTech**: (사)1EdTech Korea 설립 이사회 · 1EdTech Contributing Member.
- **연락처**: 02-2205-1470, manager@wickedstorm.kr, 서울시 강서구 마곡중앙로 161-8, A동 901~905호.
- **"누적 사용자 240만 명"**: 사업명(똑똑! 수학탐험대)과 함께 레퍼런스에서만 씁니다. 히어로에는 쓰지 않습니다.

## 문구
- 보이는 문구에 긴 줄표(—)를 쓰지 않습니다. 쉼표, 가운뎃점(·), 콜론으로 대신합니다.
- "(개발 중)" 같은 표기를 쓰지 않습니다. 화면 속 시연용 수치를 성과처럼 쓰지 않습니다.
- 제품명은 Lecognizer, Lecognizer AI, LearnHubble AI. GROWA·LXP를 제품명처럼 쓰지 않습니다.
- 주체는 운영자·교수자·학습자. "기록"보다 "학습데이터".
- AI는 찾고, 분석하고, 제안합니다. 판단과 적용은 사람이 합니다.
- 뜻이 같은 것을 언어마다 다른 단어로 쓰지 않습니다. 용어는 카탈로그 다국어판 기준입니다(homepage_renewal `i18n/*_terms.md`).

## 색
- **브랜드 그라디언트는 하나**: 마젠타 #e930b0 → 보라 #7c4dff(52%) → 파랑 #2f7cff. 글자 그라디언트는 모두 이것입니다.
- **제품 색은 그 조각**:
  - Lecognizer·Lecognizer AI는 마젠타 쪽: `linear-gradient(92deg,#e930b0,#9a4bf0)`
  - LearnHubble AI는 파랑 쪽: `linear-gradient(92deg,#6d56ff,#2f7cff)`
- **작은 강조색**(링크, 라이브 점, 완료 표시)은 #a3b1ff. 청록은 쓰지 않습니다.
- **어두운 톤이 기본입니다**. 배경 #06070e, 패널 #0e1020.

## 줄바꿈과 다국어
- 의미·단어 단위로 끊습니다. 마지막 줄에 한 단어만 남기지 않습니다. 글자는 칸을 채우고, 오른쪽에 어색한 빈 공간을 남기지 않습니다.
- **언어별 규칙**:
  - 한국어: `word-break: keep-all`
  - 일본어: keep-all 금지. 기본은 글자 단위 + `line-break: strict`이고, 제목은 빌드할 때 BudouX로 구 단위를 미리 나눕니다.
  - 베트남어: 성조 부호 때문에 줄간격을 넓힙니다.
- **언어 선택**:
  - 원어 이름으로 씁니다: 한국어 · English · 日本語 · Tiếng Việt.
  - 자동으로 다른 언어 페이지로 보내지 않고, 안내 띠만 띄웁니다.

## 결정 대기 (답을 받기 전까지 권장안으로 진행)
1. 호스팅: Cloudflare Pages(권장) 또는 GitHub Pages
2. '학습의 순간' 그림체: 기업영상의 3D 톤(권장)
3. HTML로 다시 그릴 제품 화면: 핵심 네 장면(권장). Statement 목록, 이상 신호 차트, 교수자 개입, 학습자 AI 힌트.
4. 공개 시점: 하노이 뒤 11월(권장)
5. 웹 접근성 품질인증: 공개 뒤 신청(권장)
6. 베트남 방문자용 Zalo 문의 채널: 미정

## 작업 방식
- 작업은 기능 단위 브랜치와 `main` 대상 PR로 냅니다. 병합은 사람이 정합니다.
- 화면을 바꾸면 폰·태블릿·데스크톱에서 캡처해 확인하고, PR에 붙입니다.
- 지적받은 한 곳만 고치지 말고, 같은 원인을 전체에서 찾아 고칩니다.
- 강의실 화면의 동영상 안 일본어 슬라이드는 보이지 않게 합니다(잘라 내거나 HTML로 다시 그림).
